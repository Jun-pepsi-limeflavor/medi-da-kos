# 원장 구현 계획 — buyers · suppliers

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 바이어와 제조사 원장을 서버 전용으로 만들고, 수기로 등록·수정할 수 있는 화면을 붙인다.

**Architecture:** 읽기는 서버 컴포넌트가 Admin SDK로 직접 하고, 쓰기는 `withAdmin` route handler를 지난다. 어드민 페이지 전체가 **서버 컴포넌트 레이아웃 하나 아래**에 들어가고 그 레이아웃이 세션을 검증한다 — 페이지마다 가드를 붙이는 게 아니라 중첩 레이아웃이 구조적으로 보장한다. 입력 검증은 Zod 스키마 하나가 서버와 타입 양쪽을 담당한다.

**Tech Stack:** Next.js 16 App Router (Route Group) / `firebase-admin/firestore` / Zod / `node --test`

**Spec:** `docs/backoffice-spec.md` — 4.1, 5장, 9장 작업 4

**선행:** 계획 1 (인가 게이트) 머지 완료

---

## Global Constraints

계획 1의 Global Constraints를 그대로 상속한다. 이 계획에서 특히 걸리는 것:

- 어드민은 Firebase **클라이언트 SDK를 쓰지 않는다.** 서버 컴포넌트는 Admin SDK, 클라이언트는 `fetch`
- `buyers`·`suppliers` 규칙은 **모든 클라이언트에게 `if false`** (계획 1 Task 3에서 이미 적용됨). 이 계획은 규칙을 건드리지 않는다
- 원가·마진 필드는 `src/app/admin/**`·`src/app/api/admin/**` 밖에 나타나면 안 된다 — 이 계획에는 원가가 없다(계획 5에서 들어온다)
- **`git add -A`를 쓰지 않는다.** `src/components/crm/` 프로토타입이 작업 트리에 미추적으로 남아 있고 계획 5가 쓴다

```bash
git switch -c feat/ledgers origin/dev
```

---

## File Structure

| 파일 | 책임 |
|---|---|
| `src/lib/firebase-admin.ts` (수정) | `getAdminDb()` 추가 |
| `src/lib/admin-page.ts` (신규) | 서버 컴포넌트용 세션 가드 |
| `src/app/admin/(dash)/layout.tsx` (신규) | 가드 + 어드민 셸. **모든 어드민 페이지가 이 아래 들어간다** |
| `src/app/admin/(dash)/page.tsx` (신규) | 대시보드 홈 (지금은 링크 목록) |
| `src/lib/schemas/supplier.ts` (신규) | Zod 스키마 + 타입 |
| `src/lib/schemas/buyer.ts` (신규) | Zod 스키마 + 타입 |
| `src/lib/repo/suppliers.ts` (신규) | Firestore 접근. 서버 전용 |
| `src/lib/repo/buyers.ts` (신규) | Firestore 접근. 서버 전용 |
| `src/app/api/admin/suppliers/route.ts` (신규) | 생성 |
| `src/app/api/admin/suppliers/[id]/route.ts` (신규) | 수정 |
| `src/app/api/admin/buyers/route.ts` (신규) | 생성 |
| `src/app/api/admin/buyers/[id]/route.ts` (신규) | 수정 + 이메일 추가 |
| `src/app/admin/(dash)/suppliers/page.tsx` (신규) | 목록 |
| `src/app/admin/(dash)/suppliers/SupplierForm.tsx` (신규) | 등록·수정 폼 |
| `src/app/admin/(dash)/buyers/page.tsx` (신규) | 목록 |
| `src/app/admin/(dash)/buyers/BuyerForm.tsx` (신규) | 등록·수정 폼 |
| `tests/schemas.test.ts` (신규) | 스키마 경계 검증 |

**Route Group `(dash)`가 URL에 나타나지 않는다.** `src/app/admin/(dash)/page.tsx` → `/admin`. `src/app/admin/login/page.tsx`는 그룹 밖이라 레이아웃 가드를 받지 않는다 — 받으면 로그인 페이지가 자기 자신으로 리디렉트되는 무한 루프가 된다.

기존 프로토타입 `src/app/admin/layout.tsx`·`page.tsx`(미추적)는 Task 1에서 `(dash)/` 안으로 옮겨 재사용한다.

---

### Task 1: 어드민 셸이 곧 게이트

**Files:**
- Modify: `src/lib/firebase-admin.ts`
- Create: `src/lib/admin-page.ts`
- Create: `src/app/admin/(dash)/layout.tsx`
- Create: `src/app/admin/(dash)/page.tsx`

**Interfaces:**
- Consumes: `getAdminAuth()`, `assertAllowedAdmin()`, `NotAdminError`, `ADMIN_SESSION_COOKIE`, `AdminIdentity` (계획 1)
- Produces:
  - `getAdminDb(): Firestore` — `firebase-admin/firestore`의 `Firestore`. Task 2~5가 쓴다
  - `requireAdminPage(): Promise<AdminIdentity>` — 실패 시 `/admin/login`으로 redirect. 서버 컴포넌트 전용

- [ ] **Step 1: `getAdminDb()` 추가**

`src/lib/firebase-admin.ts` 맨 아래에 추가한다. 기존 `getAdminApp()`·`getAdminAuth()`는 그대로 둔다.

```typescript
import { getFirestore, type Firestore } from "firebase-admin/firestore";

export function getAdminDb(): Firestore {
  return getFirestore(getAdminApp());
}
```

`import` 문은 파일 상단의 기존 import 옆에 둔다.

- [ ] **Step 2: 서버 컴포넌트 가드 작성**

`src/lib/admin-page.ts`:

```typescript
import "server-only";
import { cookies } from "next/headers";
import { redirect } from "next/navigation";
import { getAdminAuth } from "@/lib/firebase-admin";
import {
  ADMIN_SESSION_COOKIE,
  assertAllowedAdmin,
  NotAdminError,
  type AdminIdentity,
} from "@/lib/admin-auth";

/**
 * 서버 컴포넌트용 세션 가드.
 * withAdmin 과 판정 로직은 같지만 실패 처리가 다르다 —
 * 라우트는 401 을 주고 페이지는 로그인으로 보낸다.
 */
export async function requireAdminPage(): Promise<AdminIdentity> {
  const store = await cookies();          // Next 16 에서 cookies() 는 비동기다
  const cookie = store.get(ADMIN_SESSION_COOKIE)?.value;
  if (!cookie) redirect("/admin/login");

  let decoded;
  try {
    decoded = await getAdminAuth().verifySessionCookie(cookie, true);
  } catch {
    redirect("/admin/login");
  }

  try {
    return assertAllowedAdmin(decoded);
  } catch (err) {
    if (err instanceof NotAdminError) redirect("/admin/login");
    throw err;   // BACKOFFICE_ADMIN_EMAILS 누락 → 500
  }
}
```

`redirect()`는 예외를 던져 흐름을 끊으므로 뒤에 `return`이 필요 없다.

- [ ] **Step 3: 기존 프로토타입 셸을 그룹 안으로 옮기기**

```bash
mkdir -p "src/app/admin/(dash)"
git mv --force src/app/admin/layout.tsx "src/app/admin/(dash)/layout.tsx" 2>/dev/null \
  || mv src/app/admin/layout.tsx "src/app/admin/(dash)/layout.tsx"
mv src/app/admin/page.tsx "src/app/admin/(dash)/page.tsx"
```

미추적 파일이라 `git mv`가 실패할 수 있어 `mv` 폴백을 둔다.

- [ ] **Step 4: 레이아웃을 서버 컴포넌트 가드로 바꾸기**

`src/app/admin/(dash)/layout.tsx`를 통째로 교체한다. 기존 헤더 마크업은 유지하되 하드코딩된 `Thomas (PM) admin` 배지를 실제 세션으로 바꾼다.

```tsx
import React from "react";
import Link from "next/link";
import { LayoutDashboard, Factory, Users, ShieldCheck } from "lucide-react";
import { requireAdminPage } from "@/lib/admin-page";
import LogoutButton from "./LogoutButton";

export default async function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const actor = await requireAdminPage();

  return (
    <div className="min-h-screen bg-neutral-950 text-neutral-100 flex flex-col font-sans antialiased">
      <header className="border-b border-neutral-800 bg-neutral-900/90 backdrop-blur-md sticky top-0 z-40">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between">
          <div className="flex items-center gap-6">
            <Link href="/admin" className="flex items-center gap-2">
              <span className="font-bold text-lg text-white tracking-tight">Medidakos</span>
              <span className="bg-indigo-950 text-indigo-400 border border-indigo-800 text-[10px] font-semibold px-2 py-0.5 rounded-full uppercase">
                Backoffice
              </span>
            </Link>
            <nav className="hidden md:flex items-center gap-1 text-xs font-medium text-neutral-400">
              <Link href="/admin" className="px-3 py-2 rounded-md hover:text-white hover:bg-neutral-800 flex items-center gap-1.5">
                <LayoutDashboard className="w-4 h-4" /> 대시보드
              </Link>
              <Link href="/admin/buyers" className="px-3 py-2 rounded-md hover:text-white hover:bg-neutral-800 flex items-center gap-1.5">
                <Users className="w-4 h-4" /> 바이어
              </Link>
              <Link href="/admin/suppliers" className="px-3 py-2 rounded-md hover:text-white hover:bg-neutral-800 flex items-center gap-1.5">
                <Factory className="w-4 h-4" /> 제조사
              </Link>
            </nav>
          </div>

          <div className="flex items-center gap-2 text-xs">
            <span className="flex items-center gap-2 bg-neutral-800 px-3 py-1.5 rounded-lg border border-neutral-700">
              <ShieldCheck className="w-4 h-4 text-emerald-400" />
              <span className="text-neutral-200">{actor.email}</span>
            </span>
            <LogoutButton />
          </div>
        </div>
      </header>

      <main className="flex-1 max-w-7xl w-full mx-auto px-4 sm:px-6 lg:px-8 py-6">
        {children}
      </main>
    </div>
  );
}
```

- [ ] **Step 5: 로그아웃 버튼**

`src/app/admin/(dash)/LogoutButton.tsx`:

```tsx
"use client";

import { useRouter } from "next/navigation";

export default function LogoutButton() {
  const router = useRouter();
  return (
    <button
      type="button"
      onClick={async () => {
        await fetch("/api/admin/session", { method: "DELETE" });
        router.replace("/admin/login");
      }}
      className="px-3 py-1.5 rounded-lg border border-neutral-700 text-neutral-400 hover:text-white hover:bg-neutral-800"
    >
      로그아웃
    </button>
  );
}
```

- [ ] **Step 6: 대시보드 홈을 임시 링크 목록으로**

`src/app/admin/(dash)/page.tsx`를 교체한다. 프로토타입 칸반은 계획 5에서 되살린다.

```tsx
import Link from "next/link";

export default function AdminHomePage() {
  return (
    <div className="space-y-4">
      <h1 className="text-lg font-semibold">백오피스</h1>
      <ul className="space-y-2 text-sm">
        <li><Link href="/admin/buyers" className="text-indigo-400 hover:underline">바이어 원장</Link></li>
        <li><Link href="/admin/suppliers" className="text-indigo-400 hover:underline">제조사 원장</Link></li>
      </ul>
    </div>
  );
}
```

- [ ] **Step 7: 브라우저에서 게이트 확인**

```bash
npm run dev
```

| 확인 | 기대 |
|---|---|
| 쿠키 없이 `/admin` | `/admin/login`으로 |
| 로그인 후 `/admin` | 셸이 뜨고 헤더에 **내 이메일**이 보인다 |
| 쿠키 값을 아무 문자열로 바꾸고 `/admin` | `/admin/login`으로 (미들웨어는 통과하지만 레이아웃이 막는다) |
| 로그아웃 클릭 | 로그인 화면으로, 다시 `/admin` 가면 막힌다 |

**세 번째 줄이 이 Task의 존재 이유다.** 미들웨어는 쿠키 유무만 보므로 위조 쿠키를 통과시킨다. 레이아웃이 실제로 검증하는지를 여기서 확인한다.

- [ ] **Step 8: 커밋**

```bash
git add src/lib/firebase-admin.ts src/lib/admin-page.ts "src/app/admin/(dash)"
git commit -m "feat(admin): 레이아웃이 세션을 검증하는 어드민 셸

페이지마다 가드를 붙이지 않는다. 중첩 레이아웃 하나가 그 아래 전부를 덮는다."
```

---

### Task 2: suppliers 스키마와 저장소

**Files:**
- Create: `src/lib/schemas/supplier.ts`
- Create: `src/lib/repo/suppliers.ts`
- Create: `src/app/api/admin/suppliers/route.ts`
- Create: `src/app/api/admin/suppliers/[id]/route.ts`
- Test: `tests/schemas.test.ts`

**Interfaces:**
- Consumes: `getAdminDb()` (Task 1), `withAdmin` (계획 1)
- Produces:
  - `supplierInputSchema` — Zod 스키마
  - `type SupplierInput = z.infer<typeof supplierInputSchema>`
  - `type Supplier = SupplierInput & { id: string; createdAt: string; updatedAt: string }`
  - `listSuppliers(): Promise<Supplier[]>`, `createSupplier(input, actor): Promise<string>`, `updateSupplier(id, input, actor): Promise<void>`

- [ ] **Step 1: Zod 설치**

```bash
npm i --save-exact zod@4.1.13
```

설치 후 `node -p "require('./node_modules/zod/package.json').version"`으로 확인한다. 아래 코드는 `z.object`·`z.string`·`z.enum`·`z.array`·`.refine`·`z.infer`만 쓰므로 zod 3·4 양쪽에서 같다. `z.string().email()`은 버전 간 차이가 있어 쓰지 않는다.

- [ ] **Step 2: 실패하는 스키마 테스트 작성**

`tests/schemas.test.ts`:

```typescript
import { test } from "node:test";
import assert from "node:assert/strict";
import { supplierInputSchema } from "../src/lib/schemas/supplier.ts";

const valid = {
  companyName: "그린코스",
  contactPerson: "김대리",
  phone: "02-1234-5678",
  email: "sales@greencos.co.kr",
  category: "ODM" as const,
  supportedCerts: ["ISO 22716", "CGMP"],
  ownershipExclusivity: "",
};

test("정상 입력을 통과시킨다", () => {
  const parsed = supplierInputSchema.parse(valid);
  assert.equal(parsed.companyName, "그린코스");
  assert.deepEqual(parsed.supportedCerts, ["ISO 22716", "CGMP"]);
});

test("회사명 앞뒤 공백을 다듬는다", () => {
  const parsed = supplierInputSchema.parse({ ...valid, companyName: "  그린코스  " });
  assert.equal(parsed.companyName, "그린코스");
});

test("이메일을 소문자로 내린다", () => {
  const parsed = supplierInputSchema.parse({ ...valid, email: "Sales@GreenCos.co.KR" });
  assert.equal(parsed.email, "sales@greencos.co.kr");
});

test("회사명이 비면 거부한다", () => {
  assert.throws(() => supplierInputSchema.parse({ ...valid, companyName: "   " }));
});

test("category 는 pack 과 ODM 만 받는다", () => {
  assert.throws(() => supplierInputSchema.parse({ ...valid, category: "OEM" }));
});

test("@ 없는 이메일을 거부한다", () => {
  assert.throws(() => supplierInputSchema.parse({ ...valid, email: "sales.greencos.co.kr" }));
});

test("인증 목록은 생략 가능하고 빈 배열이 된다", () => {
  const { supportedCerts, ...rest } = valid;
  const parsed = supplierInputSchema.parse(rest);
  assert.deepEqual(parsed.supportedCerts, []);
});

test("모르는 필드는 떨어뜨린다", () => {
  const parsed = supplierInputSchema.parse({ ...valid, unitCost: 1200 }) as Record<string, unknown>;
  assert.equal(parsed.unitCost, undefined);
});
```

마지막 테스트가 중요하다. 폼에서 넘어온 잉여 필드가 그대로 Firestore에 들어가면 **원가 필드가 의도치 않게 원장에 섞인다.** Zod의 기본 동작이 미지 필드를 떨어뜨리는 것이지만, 그게 유지되는지를 테스트가 지킨다.

- [ ] **Step 3: 실행해서 실패 확인**

Run: `npm test`
Expected: `schemas.test.ts` 전건 FAIL — 모듈 없음

- [ ] **Step 4: 스키마 작성**

`src/lib/schemas/supplier.ts`:

```typescript
import { z } from "zod";

const trimmed = z.string().transform((s) => s.trim());
const required = trimmed.refine((s) => s.length > 0, { message: "필수 항목입니다" });
const email = trimmed
  .transform((s) => s.toLowerCase())
  .refine((s) => s.includes("@") && s.length >= 5, { message: "이메일 형식이 아닙니다" });

export const supplierInputSchema = z.object({
  companyName: required,
  contactPerson: required,
  phone: required,
  email,
  category: z.enum(["pack", "ODM"]),
  supportedCerts: z.array(trimmed).default([]),
  ownershipExclusivity: trimmed.default(""),
});

export type SupplierInput = z.infer<typeof supplierInputSchema>;

export type Supplier = SupplierInput & {
  id: string;
  createdAt: string;
  updatedAt: string;
};
```

- [ ] **Step 5: 실행해서 통과 확인**

Run: `npm test`
Expected: `schemas.test.ts` 8건 PASS

- [ ] **Step 6: 저장소 작성**

`src/lib/repo/suppliers.ts`:

```typescript
import "server-only";
import { getAdminDb } from "@/lib/firebase-admin";
import type { AdminIdentity } from "@/lib/admin-auth";
import type { Supplier, SupplierInput } from "@/lib/schemas/supplier";

const COLLECTION = "suppliers";

export async function listSuppliers(): Promise<Supplier[]> {
  const snap = await getAdminDb().collection(COLLECTION).orderBy("companyName").get();
  return snap.docs.map((d) => ({ id: d.id, ...d.data() }) as Supplier);
}

export async function findSupplierByName(companyName: string): Promise<Supplier | null> {
  const snap = await getAdminDb()
    .collection(COLLECTION)
    .where("companyName", "==", companyName)
    .limit(1)
    .get();
  const doc = snap.docs[0];
  return doc ? ({ id: doc.id, ...doc.data() } as Supplier) : null;
}

export async function createSupplier(
  input: SupplierInput,
  actor: AdminIdentity,
): Promise<string> {
  const existing = await findSupplierByName(input.companyName);
  if (existing) {
    throw new DuplicateSupplierError(input.companyName);
  }
  const now = new Date().toISOString();
  const ref = await getAdminDb().collection(COLLECTION).add({
    ...input,
    createdAt: now,
    updatedAt: now,
    createdBy: actor.email,
  });
  return ref.id;
}

export async function updateSupplier(
  id: string,
  input: SupplierInput,
  actor: AdminIdentity,
): Promise<void> {
  await getAdminDb().collection(COLLECTION).doc(id).update({
    ...input,
    updatedAt: new Date().toISOString(),
    updatedBy: actor.email,
  });
}

export class DuplicateSupplierError extends Error {
  constructor(companyName: string) {
    super(`supplier already exists: ${companyName}`);
  }
}
```

문서 ID는 자동 생성이다. 회사명을 ID로 쓰지 않는 이유는 스펙 4장에 있다 — 회사명은 바뀌고 표기가 흔들린다(주식회사·㈜·영문명). 대신 `findSupplierByName`으로 중복만 막는다.

- [ ] **Step 7: 생성 라우트**

`src/app/api/admin/suppliers/route.ts`:

```typescript
import { NextResponse } from "next/server";
import { withAdmin } from "@/lib/with-admin";
import { supplierInputSchema } from "@/lib/schemas/supplier";
import { createSupplier, DuplicateSupplierError } from "@/lib/repo/suppliers";

export const runtime = "nodejs";

export const POST = withAdmin(async (req, actor) => {
  const body = await req.json().catch(() => null);
  const parsed = supplierInputSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json(
      { error: "invalid input", issues: parsed.error.issues },
      { status: 400 },
    );
  }
  try {
    const id = await createSupplier(parsed.data, actor);
    return NextResponse.json({ id }, { status: 201 });
  } catch (err) {
    if (err instanceof DuplicateSupplierError) {
      return NextResponse.json({ error: "duplicate" }, { status: 409 });
    }
    throw err;
  }
});
```

- [ ] **Step 8: 수정 라우트**

`src/app/api/admin/suppliers/[id]/route.ts`:

```typescript
import { NextResponse } from "next/server";
import { withAdmin } from "@/lib/with-admin";
import { supplierInputSchema } from "@/lib/schemas/supplier";
import { updateSupplier } from "@/lib/repo/suppliers";

export const runtime = "nodejs";

export const PUT = withAdmin(async (req, actor) => {
  const id = req.nextUrl.pathname.split("/").filter(Boolean).pop();
  if (!id) return NextResponse.json({ error: "id required" }, { status: 400 });

  const body = await req.json().catch(() => null);
  const parsed = supplierInputSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json(
      { error: "invalid input", issues: parsed.error.issues },
      { status: 400 },
    );
  }
  await updateSupplier(id, parsed.data, actor);
  return NextResponse.json({ ok: true });
});
```

`withAdmin`의 시그니처가 `(req, actor)`라 Next의 두 번째 인자(`{ params }`)를 받지 않는다. 그래서 경로에서 `id`를 직접 꺼낸다. 래퍼를 바꾸지 않는 쪽을 택했다 — 래퍼가 단순할수록 잊어버릴 여지가 없다.

- [ ] **Step 9: 누락 검사와 타입체크**

Run: `npm test && npm run typecheck && npm run lint`
Expected: 전부 통과. `with-admin-coverage.test.ts`가 새 라우트 둘도 검사한다.

- [ ] **Step 10: 커밋**

```bash
git add package.json package-lock.json src/lib/schemas/supplier.ts src/lib/repo/suppliers.ts src/app/api/admin/suppliers tests/schemas.test.ts
git commit -m "feat(admin): 제조사 원장 스키마와 서버 CRUD"
```

---

### Task 3: suppliers 화면

**Files:**
- Create: `src/app/admin/(dash)/suppliers/page.tsx`
- Create: `src/app/admin/(dash)/suppliers/SupplierForm.tsx`

**Interfaces:**
- Consumes: `listSuppliers()`, `supplierInputSchema`, `type Supplier`
- Produces: 없음

- [ ] **Step 1: 목록 페이지 (서버 컴포넌트)**

`src/app/admin/(dash)/suppliers/page.tsx`:

```tsx
import { listSuppliers } from "@/lib/repo/suppliers";
import SupplierForm from "./SupplierForm";

export const dynamic = "force-dynamic";

export default async function SuppliersPage() {
  const suppliers = await listSuppliers();

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between border-b border-neutral-800 pb-4">
        <h1 className="text-lg font-semibold">제조사 {suppliers.length}곳</h1>
        <SupplierForm />
      </div>

      {suppliers.length === 0 ? (
        <p className="text-sm text-neutral-500">등록된 제조사가 없습니다.</p>
      ) : (
        <div className="overflow-x-auto">
          <table className="w-full text-sm">
            <thead className="text-xs text-neutral-500 border-b border-neutral-800">
              <tr>
                <th className="text-left py-2 font-medium">회사명</th>
                <th className="text-left py-2 font-medium">구분</th>
                <th className="text-left py-2 font-medium">담당자</th>
                <th className="text-left py-2 font-medium">연락처</th>
                <th className="text-left py-2 font-medium">대응 인증</th>
                <th />
              </tr>
            </thead>
            <tbody className="divide-y divide-neutral-900">
              {suppliers.map((s) => (
                <tr key={s.id}>
                  <td className="py-2.5 font-medium text-neutral-100">{s.companyName}</td>
                  <td className="py-2.5">
                    <span className="text-[10px] uppercase px-2 py-0.5 rounded-full border border-neutral-700 text-neutral-400">
                      {s.category}
                    </span>
                  </td>
                  <td className="py-2.5 text-neutral-300">{s.contactPerson}</td>
                  <td className="py-2.5 text-neutral-400">
                    <div>{s.phone}</div>
                    <div className="text-xs">{s.email}</div>
                  </td>
                  <td className="py-2.5 text-xs text-neutral-400">
                    {s.supportedCerts.length ? s.supportedCerts.join(", ") : "—"}
                  </td>
                  <td className="py-2.5 text-right">
                    <SupplierForm supplier={s} />
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </div>
  );
}
```

`export const dynamic = "force-dynamic"`가 필요하다. 빼면 Next가 이 페이지를 빌드 시점에 정적으로 렌더하려 하고, 그때는 세션도 Firestore 자격증명도 없다.

- [ ] **Step 2: 폼 (클라이언트 컴포넌트)**

`src/app/admin/(dash)/suppliers/SupplierForm.tsx`:

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import type { Supplier } from "@/lib/schemas/supplier";

const EMPTY = {
  companyName: "", contactPerson: "", phone: "", email: "",
  category: "ODM", supportedCerts: "", ownershipExclusivity: "",
};

export default function SupplierForm({ supplier }: { supplier?: Supplier }) {
  const router = useRouter();
  const [open, setOpen] = useState(false);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [form, setForm] = useState(
    supplier
      ? { ...supplier, supportedCerts: supplier.supportedCerts.join(", ") }
      : EMPTY,
  );

  function set(key: string, value: string) {
    setForm((f) => ({ ...f, [key]: value }));
  }

  async function submit(e: React.FormEvent) {
    e.preventDefault();
    setBusy(true);
    setError(null);

    const payload = {
      companyName: form.companyName,
      contactPerson: form.contactPerson,
      phone: form.phone,
      email: form.email,
      category: form.category,
      supportedCerts: form.supportedCerts.split(",").map((s) => s.trim()).filter(Boolean),
      ownershipExclusivity: form.ownershipExclusivity,
    };

    const res = await fetch(
      supplier ? `/api/admin/suppliers/${supplier.id}` : "/api/admin/suppliers",
      {
        method: supplier ? "PUT" : "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify(payload),
      },
    );
    setBusy(false);

    if (res.status === 409) return setError("같은 회사명이 이미 등록돼 있습니다.");
    if (res.status === 400) return setError("입력을 확인해주세요.");
    if (!res.ok) return setError("저장에 실패했습니다.");

    setOpen(false);
    router.refresh();
  }

  if (!open) {
    return (
      <button
        type="button"
        onClick={() => setOpen(true)}
        className="text-xs px-3 py-1.5 rounded-lg border border-neutral-700 text-neutral-300 hover:bg-neutral-800"
      >
        {supplier ? "수정" : "제조사 등록"}
      </button>
    );
  }

  return (
    <div className="fixed inset-0 z-50 bg-black/70 flex items-center justify-center p-4">
      <form
        onSubmit={submit}
        className="w-full max-w-lg bg-neutral-900 border border-neutral-800 rounded-xl p-5 space-y-3"
      >
        <h2 className="text-sm font-semibold">{supplier ? "제조사 수정" : "제조사 등록"}</h2>

        {[
          ["companyName", "회사명", true],
          ["contactPerson", "담당자", true],
          ["phone", "전화번호", true],
          ["email", "이메일", true],
          ["supportedCerts", "대응 인증 (쉼표로 구분)", false],
          ["ownershipExclusivity", "소유권·독점권", false],
        ].map(([key, label, req]) => (
          <label key={key as string} className="block">
            <span className="text-xs text-neutral-400">{label as string}</span>
            <input
              value={form[key as keyof typeof form] as string}
              onChange={(e) => set(key as string, e.target.value)}
              required={req as boolean}
              className="mt-1 w-full rounded-lg bg-neutral-950 border border-neutral-700 px-3 py-2 text-sm"
            />
          </label>
        ))}

        <label className="block">
          <span className="text-xs text-neutral-400">구분</span>
          <select
            value={form.category}
            onChange={(e) => set("category", e.target.value)}
            className="mt-1 w-full rounded-lg bg-neutral-950 border border-neutral-700 px-3 py-2 text-sm"
          >
            <option value="ODM">ODM — 제형·처방</option>
            <option value="pack">pack — 용기·부자재</option>
          </select>
        </label>

        {error && <p role="alert" className="text-xs text-red-400">{error}</p>}

        <div className="flex justify-end gap-2 pt-1">
          <button
            type="button"
            onClick={() => { setOpen(false); setError(null); }}
            className="text-xs px-3 py-1.5 rounded-lg text-neutral-400 hover:bg-neutral-800"
          >
            취소
          </button>
          <button
            type="submit"
            disabled={busy}
            className="text-xs px-3 py-1.5 rounded-lg bg-indigo-600 text-white disabled:opacity-50"
          >
            {busy ? "저장 중…" : "저장"}
          </button>
        </div>
      </form>
    </div>
  );
}
```

- [ ] **Step 3: 브라우저에서 확인**

| 확인 | 기대 |
|---|---|
| 제조사 등록 → 저장 | 목록에 나타난다 |
| 같은 회사명으로 또 등록 | "같은 회사명이 이미 등록돼 있습니다" |
| 회사명을 공백만으로 등록 | 브라우저가 막고, 우회해도 400 |
| 수정 → 인증 목록 변경 | 목록에 반영된다 |
| **Firebase 콘솔에서 `suppliers` 확인** | 문서가 실제로 생겼고 `createdBy`에 내 이메일이 있다 |

마지막 줄을 반드시 확인한다. 화면이 성공을 표시해도 Firestore에 안 들어갔을 수 있다.

- [ ] **Step 4: 커밋**

```bash
git add "src/app/admin/(dash)/suppliers"
git commit -m "feat(admin): 제조사 목록과 등록·수정 폼"
```

---

### Task 4: buyers 스키마와 저장소

**Files:**
- Create: `src/lib/schemas/buyer.ts`
- Create: `src/lib/repo/buyers.ts`
- Create: `src/app/api/admin/buyers/route.ts`
- Create: `src/app/api/admin/buyers/[id]/route.ts`
- Modify: `tests/schemas.test.ts`

**Interfaces:**
- Consumes: `getAdminDb()`, `withAdmin`
- Produces:
  - `buyerInputSchema`, `type BuyerInput`, `type Buyer`
  - `listBuyers()`, `createBuyer(input, actor)`, `updateBuyer(id, input, actor)`, `findBuyerByEmail(email)`

- [ ] **Step 1: 실패하는 테스트 추가**

`tests/schemas.test.ts` 아래에 이어 붙인다.

```typescript
import { buyerInputSchema } from "../src/lib/schemas/buyer.ts";

const validBuyer = {
  name: "Charity Kobia",
  emails: ["candy@example.com"],
  inflowChannel: "outlook" as const,
  brandName: "TJ perfumes",
  country: "Kenya",
  phone: "",
};

test("바이어 정상 입력을 통과시킨다", () => {
  const parsed = buyerInputSchema.parse(validBuyer);
  assert.deepEqual(parsed.emails, ["candy@example.com"]);
});

test("이메일 배열을 소문자로 내리고 중복을 없앤다", () => {
  const parsed = buyerInputSchema.parse({
    ...validBuyer,
    emails: ["Candy@Example.com", "candy@example.com", "  CANDY@EXAMPLE.COM  "],
  });
  assert.deepEqual(parsed.emails, ["candy@example.com"]);
});

test("이메일이 하나도 없으면 거부한다", () => {
  assert.throws(() => buyerInputSchema.parse({ ...validBuyer, emails: [] }));
});

test("이름이 비면 거부한다", () => {
  assert.throws(() => buyerInputSchema.parse({ ...validBuyer, name: "  " }));
});

test("모르는 유입경로를 거부한다", () => {
  assert.throws(() => buyerInputSchema.parse({ ...validBuyer, inflowChannel: "instagram" }));
});
```

두 번째 테스트가 핵심이다. **한 사람이 여러 주소를 쓰는 상황이 이미 발생 중**이라 이메일이 배열이고, 배열이면 중복이 들어온다.

- [ ] **Step 2: 실행해서 실패 확인**

Run: `npm test`
Expected: 새 5건 FAIL — 모듈 없음

- [ ] **Step 3: 스키마 작성**

`src/lib/schemas/buyer.ts`:

```typescript
import { z } from "zod";

const trimmed = z.string().transform((s) => s.trim());
const required = trimmed.refine((s) => s.length > 0, { message: "필수 항목입니다" });

export const INFLOW_CHANNELS = [
  "gmail_thomas", "gmail_hally", "outlook", "website",
  "channel_talk", "coldmail", "manual",
] as const;

export const buyerInputSchema = z.object({
  name: required,
  emails: z
    .array(trimmed.transform((s) => s.toLowerCase()))
    .transform((list) => [...new Set(list.filter((e) => e.includes("@")))])
    .refine((list) => list.length > 0, { message: "이메일이 최소 하나 필요합니다" }),
  inflowChannel: z.enum(INFLOW_CHANNELS),
  brandName: trimmed.default(""),
  country: trimmed.default(""),
  phone: trimmed.default(""),
});

export type BuyerInput = z.infer<typeof buyerInputSchema>;

export type Buyer = BuyerInput & {
  id: string;
  firebaseUid?: string;
  lastContactAt?: string;
  createdAt: string;
  updatedAt: string;
};
```

- [ ] **Step 4: 통과 확인**

Run: `npm test`
Expected: `schemas.test.ts` 13건 PASS

- [ ] **Step 5: 저장소 작성**

`src/lib/repo/buyers.ts`:

```typescript
import "server-only";
import { getAdminDb } from "@/lib/firebase-admin";
import type { AdminIdentity } from "@/lib/admin-auth";
import type { Buyer, BuyerInput } from "@/lib/schemas/buyer";

const COLLECTION = "buyers";

export async function listBuyers(): Promise<Buyer[]> {
  const snap = await getAdminDb().collection(COLLECTION).orderBy("name").get();
  return snap.docs.map((d) => ({ id: d.id, ...d.data() }) as Buyer);
}

/** 수집기가 발신자 주소로 바이어를 찾을 때 쓴다. */
export async function findBuyerByEmail(email: string): Promise<Buyer | null> {
  const snap = await getAdminDb()
    .collection(COLLECTION)
    .where("emails", "array-contains", email.trim().toLowerCase())
    .limit(1)
    .get();
  const doc = snap.docs[0];
  return doc ? ({ id: doc.id, ...doc.data() } as Buyer) : null;
}

export class EmailTakenError extends Error {
  constructor(public email: string) {
    super(`email already on another buyer: ${email}`);
  }
}

async function assertEmailsFree(emails: string[], exceptId?: string) {
  for (const email of emails) {
    const found = await findBuyerByEmail(email);
    if (found && found.id !== exceptId) throw new EmailTakenError(email);
  }
}

export async function createBuyer(
  input: BuyerInput,
  actor: AdminIdentity,
): Promise<string> {
  await assertEmailsFree(input.emails);
  const now = new Date().toISOString();
  const ref = await getAdminDb().collection(COLLECTION).add({
    ...input,
    createdAt: now,
    updatedAt: now,
    createdBy: actor.email,
  });
  return ref.id;
}

export async function updateBuyer(
  id: string,
  input: BuyerInput,
  actor: AdminIdentity,
): Promise<void> {
  await assertEmailsFree(input.emails, id);
  await getAdminDb().collection(COLLECTION).doc(id).update({
    ...input,
    updatedAt: new Date().toISOString(),
    updatedBy: actor.email,
  });
}
```

`assertEmailsFree`가 이메일마다 조회를 한 번씩 한다. 바이어 30명 규모에서 문제가 안 되고, 이메일이 두 바이어에 걸치면 수집기의 자동 연결이 어느 쪽으로 갈지 결정할 수 없어진다 — 그게 더 비싼 문제다.

```
ponytail: 이메일당 1회 조회. 바이어가 수천 명이 되면 emails 를 별도 인덱스 컬렉션으로 뺀다.
```

- [ ] **Step 6: 라우트 두 개**

`src/app/api/admin/buyers/route.ts`:

```typescript
import { NextResponse } from "next/server";
import { withAdmin } from "@/lib/with-admin";
import { buyerInputSchema } from "@/lib/schemas/buyer";
import { createBuyer, EmailTakenError } from "@/lib/repo/buyers";

export const runtime = "nodejs";

export const POST = withAdmin(async (req, actor) => {
  const body = await req.json().catch(() => null);
  const parsed = buyerInputSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json({ error: "invalid input", issues: parsed.error.issues }, { status: 400 });
  }
  try {
    const id = await createBuyer(parsed.data, actor);
    return NextResponse.json({ id }, { status: 201 });
  } catch (err) {
    if (err instanceof EmailTakenError) {
      return NextResponse.json({ error: "email taken", email: err.email }, { status: 409 });
    }
    throw err;
  }
});
```

`src/app/api/admin/buyers/[id]/route.ts`:

```typescript
import { NextResponse } from "next/server";
import { withAdmin } from "@/lib/with-admin";
import { buyerInputSchema } from "@/lib/schemas/buyer";
import { updateBuyer, EmailTakenError } from "@/lib/repo/buyers";

export const runtime = "nodejs";

export const PUT = withAdmin(async (req, actor) => {
  const id = req.nextUrl.pathname.split("/").filter(Boolean).pop();
  if (!id) return NextResponse.json({ error: "id required" }, { status: 400 });

  const body = await req.json().catch(() => null);
  const parsed = buyerInputSchema.safeParse(body);
  if (!parsed.success) {
    return NextResponse.json({ error: "invalid input", issues: parsed.error.issues }, { status: 400 });
  }
  try {
    await updateBuyer(id, parsed.data, actor);
    return NextResponse.json({ ok: true });
  } catch (err) {
    if (err instanceof EmailTakenError) {
      return NextResponse.json({ error: "email taken", email: err.email }, { status: 409 });
    }
    throw err;
  }
});
```

- [ ] **Step 7: 검사와 커밋**

Run: `npm test && npm run typecheck && npm run lint`

```bash
git add src/lib/schemas/buyer.ts src/lib/repo/buyers.ts src/app/api/admin/buyers tests/schemas.test.ts
git commit -m "feat(admin): 바이어 원장 스키마와 서버 CRUD

이메일이 배열인 이유는 한 사람이 여러 주소를 쓰는 상황이 이미 있기 때문이다."
```

---

### Task 5: buyers 화면

**Files:**
- Create: `src/app/admin/(dash)/buyers/page.tsx`
- Create: `src/app/admin/(dash)/buyers/BuyerForm.tsx`

**Interfaces:**
- Consumes: `listBuyers()`, `type Buyer`, `INFLOW_CHANNELS`
- Produces: 없음

- [ ] **Step 1: 목록 페이지**

Task 3의 `suppliers/page.tsx`와 같은 골격이다. 컬럼만 다르다.

`src/app/admin/(dash)/buyers/page.tsx`:

```tsx
import { listBuyers } from "@/lib/repo/buyers";
import BuyerForm from "./BuyerForm";

export const dynamic = "force-dynamic";

export default async function BuyersPage() {
  const buyers = await listBuyers();

  return (
    <div className="space-y-6">
      <div className="flex items-center justify-between border-b border-neutral-800 pb-4">
        <h1 className="text-lg font-semibold">바이어 {buyers.length}명</h1>
        <BuyerForm />
      </div>

      {buyers.length === 0 ? (
        <p className="text-sm text-neutral-500">등록된 바이어가 없습니다.</p>
      ) : (
        <div className="overflow-x-auto">
          <table className="w-full text-sm">
            <thead className="text-xs text-neutral-500 border-b border-neutral-800">
              <tr>
                <th className="text-left py-2 font-medium">이름</th>
                <th className="text-left py-2 font-medium">브랜드</th>
                <th className="text-left py-2 font-medium">이메일</th>
                <th className="text-left py-2 font-medium">국가</th>
                <th className="text-left py-2 font-medium">유입</th>
                <th />
              </tr>
            </thead>
            <tbody className="divide-y divide-neutral-900">
              {buyers.map((b) => (
                <tr key={b.id}>
                  <td className="py-2.5 font-medium text-neutral-100">{b.name}</td>
                  <td className="py-2.5 text-neutral-300">{b.brandName || "—"}</td>
                  <td className="py-2.5 text-neutral-400 text-xs">
                    {b.emails.map((e) => <div key={e}>{e}</div>)}
                  </td>
                  <td className="py-2.5 text-neutral-400">{b.country || "—"}</td>
                  <td className="py-2.5">
                    <span className="text-[10px] px-2 py-0.5 rounded-full border border-neutral-700 text-neutral-400">
                      {b.inflowChannel}
                    </span>
                  </td>
                  <td className="py-2.5 text-right"><BuyerForm buyer={b} /></td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 폼**

`src/app/admin/(dash)/buyers/BuyerForm.tsx` — Task 3의 `SupplierForm`과 같은 구조다. 다른 점은 **이메일이 여러 줄 입력**이라는 것뿐이다.

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { INFLOW_CHANNELS, type Buyer } from "@/lib/schemas/buyer";

const EMPTY = {
  name: "", emails: "", inflowChannel: "manual",
  brandName: "", country: "", phone: "",
};

export default function BuyerForm({ buyer }: { buyer?: Buyer }) {
  const router = useRouter();
  const [open, setOpen] = useState(false);
  const [busy, setBusy] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [form, setForm] = useState(
    buyer ? { ...buyer, emails: buyer.emails.join("\n") } : EMPTY,
  );

  function set(key: string, value: string) {
    setForm((f) => ({ ...f, [key]: value }));
  }

  async function submit(e: React.FormEvent) {
    e.preventDefault();
    setBusy(true);
    setError(null);

    const payload = {
      name: form.name,
      emails: form.emails.split("\n").map((s) => s.trim()).filter(Boolean),
      inflowChannel: form.inflowChannel,
      brandName: form.brandName,
      country: form.country,
      phone: form.phone,
    };

    const res = await fetch(
      buyer ? `/api/admin/buyers/${buyer.id}` : "/api/admin/buyers",
      {
        method: buyer ? "PUT" : "POST",
        headers: { "content-type": "application/json" },
        body: JSON.stringify(payload),
      },
    );
    setBusy(false);

    if (res.status === 409) {
      const { email } = await res.json();
      return setError(`${email} 은 다른 바이어에 등록돼 있습니다.`);
    }
    if (res.status === 400) return setError("입력을 확인해주세요.");
    if (!res.ok) return setError("저장에 실패했습니다.");

    setOpen(false);
    router.refresh();
  }

  if (!open) {
    return (
      <button
        type="button"
        onClick={() => setOpen(true)}
        className="text-xs px-3 py-1.5 rounded-lg border border-neutral-700 text-neutral-300 hover:bg-neutral-800"
      >
        {buyer ? "수정" : "바이어 등록"}
      </button>
    );
  }

  return (
    <div className="fixed inset-0 z-50 bg-black/70 flex items-center justify-center p-4">
      <form
        onSubmit={submit}
        className="w-full max-w-lg bg-neutral-900 border border-neutral-800 rounded-xl p-5 space-y-3"
      >
        <h2 className="text-sm font-semibold">{buyer ? "바이어 수정" : "바이어 등록"}</h2>

        <label className="block">
          <span className="text-xs text-neutral-400">이름</span>
          <input
            value={form.name}
            onChange={(e) => set("name", e.target.value)}
            required
            className="mt-1 w-full rounded-lg bg-neutral-950 border border-neutral-700 px-3 py-2 text-sm"
          />
        </label>

        <label className="block">
          <span className="text-xs text-neutral-400">이메일 — 한 줄에 하나</span>
          <textarea
            value={form.emails}
            onChange={(e) => set("emails", e.target.value)}
            required
            rows={3}
            className="mt-1 w-full rounded-lg bg-neutral-950 border border-neutral-700 px-3 py-2 text-sm font-mono"
          />
        </label>

        {[["brandName", "브랜드"], ["country", "국가"], ["phone", "전화번호"]].map(
          ([key, label]) => (
            <label key={key} className="block">
              <span className="text-xs text-neutral-400">{label}</span>
              <input
                value={form[key as keyof typeof form] as string}
                onChange={(e) => set(key, e.target.value)}
                className="mt-1 w-full rounded-lg bg-neutral-950 border border-neutral-700 px-3 py-2 text-sm"
              />
            </label>
          ),
        )}

        <label className="block">
          <span className="text-xs text-neutral-400">유입 경로</span>
          <select
            value={form.inflowChannel}
            onChange={(e) => set("inflowChannel", e.target.value)}
            className="mt-1 w-full rounded-lg bg-neutral-950 border border-neutral-700 px-3 py-2 text-sm"
          >
            {INFLOW_CHANNELS.map((c) => <option key={c} value={c}>{c}</option>)}
          </select>
        </label>

        {error && <p role="alert" className="text-xs text-red-400">{error}</p>}

        <div className="flex justify-end gap-2 pt-1">
          <button
            type="button"
            onClick={() => { setOpen(false); setError(null); }}
            className="text-xs px-3 py-1.5 rounded-lg text-neutral-400 hover:bg-neutral-800"
          >
            취소
          </button>
          <button
            type="submit"
            disabled={busy}
            className="text-xs px-3 py-1.5 rounded-lg bg-indigo-600 text-white disabled:opacity-50"
          >
            {busy ? "저장 중…" : "저장"}
          </button>
        </div>
      </form>
    </div>
  );
}
```

- [ ] **Step 3: 실제 바이어 몇 명을 손으로 넣어본다**

볼트에 있는 진짜 케이스를 쓴다. 스키마가 현실을 담는지 여기서 드러난다.

| 바이어 | 확인할 점 |
|---|---|
| Charity (Candy Kobia) | 이름과 표시명이 다르다 — `name`에 뭘 넣을지 |
| Division Twenty | 콜드메일 회신. `inflowChannel: coldmail` |
| Bala Subramanyam Pinnamaneni | 웹 가입 + 메일. 주소가 둘일 수 있다 |

- [ ] **Step 4: 확인**

| 확인 | 기대 |
|---|---|
| 이메일 두 줄로 등록 | 목록에 두 줄 다 보인다 |
| 같은 이메일을 다른 바이어에 등록 | "다른 바이어에 등록돼 있습니다" |
| 같은 이메일을 대소문자만 바꿔 두 줄 | 하나로 합쳐진다 |
| Firebase 콘솔 `buyers` | `emails`가 소문자 배열이다 |

- [ ] **Step 5: 커밋과 PR**

```bash
git add "src/app/admin/(dash)/buyers"
git commit -m "feat(admin): 바이어 목록과 등록·수정 폼"
git push -u origin feat/ledgers
gh pr create --base dev --title "feat(admin): 바이어·제조사 원장" --body "스펙 docs/backoffice-spec.md 4.1 / 작업 순서 4"
```

---

## 다음 계획으로 넘기는 것

- **딜 연결** — 바이어·제조사를 딜에 묶는 건 계획 5
- **원가** — `supplierContacts[]`의 `unitCost`·`moq`는 계획 5. 이 계획의 `suppliers`에는 원가가 없다
- **자동 연결** — 수집기가 발신자로 바이어를 찾는 건 계획 3. `findBuyerByEmail()`을 여기서 미리 만들어 뒀다
