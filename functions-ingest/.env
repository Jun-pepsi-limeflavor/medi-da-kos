# 운영 배포값. 비밀값이 아니므로 커밋한다 — 비밀은 Secret Manager 에 둔다.
# 코드 허용목록(google-auth.js)에 있고 enabled 인 계정만 실제로 폴링된다.
INGEST_MAILBOXES=thomas@medidakoslabs.com,hally@medidakoslabs.com,support@medidakos.com

# 신규 메일함의 최초 조회 시작점. 비우면 30일. 기존 메일함은 ingestState 를 쓰므로 영향 없다.
INGEST_INITIAL_AFTER=2026-07-29T00:00:00.000Z
